name: LOTO6 Dynamics Network Coupling v0

on:
  workflow_dispatch:
  push:
    branches:
      - probe/loto6-dynamics-network-coupling-v0
    paths:
      - 'playground/loto6-dynamics-network-coupling/**'
      - '.github/workflows/loto6-dynamics-network-coupling-v0.yml'

jobs:
  observe:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install numpy requests
      - name: Measure Dynamics × Network coupling
        run: python playground/loto6-dynamics-network-coupling/coupling_probe.py
      - uses: actions/upload-artifact@v4
        with:
          name: loto6-dynamics-network-coupling-v0
          path: results/loto6_dynamics_network_coupling_v0.json
          if-no-files-found: error
